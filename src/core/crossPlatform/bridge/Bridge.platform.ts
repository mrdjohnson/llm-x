import { type IBridge } from '~/core/crossPlatform/bridge/IBridge'

export const registerPlatformBridge = (): IBridge | undefined => {
  return undefined
}

export const getPlatformBridge = (): IBridge | undefined => {
  return undefined
}
